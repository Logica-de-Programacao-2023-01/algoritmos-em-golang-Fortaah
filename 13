package main

import (
	"fmt"
)

func main() {
	var x int
	fmt.Print("Me de 1 número para eu descobrir se ele é par ou impar")
	fmt.Scan(&x)
	if x%2 == 0 {
		fmt.Println(x, "é par")
	} else {
		fmt.Println(x, "é impar.")
	}
}
